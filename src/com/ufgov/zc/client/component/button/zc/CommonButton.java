package com.ufgov.zc.client.component.button.zc;import com.ufgov.zc.client.component.button.FuncButton;public class CommonButton extends FuncButton {  private static final long serialVersionUID = -3564446291342163428L;  public CommonButton(String funcId, String defaultText, String iconName) {    this(funcId, defaultText, iconName, true);  }  public CommonButton(String funcId, String defaultText, String iconName, boolean funcCtrl) {    this.funcId = funcId;    this.defaultText = defaultText;    this.iconName = iconName;    this.funcCtrl = funcCtrl;    super.init();  }  public CommonButton(String funcId, String iconName) {    this(funcId, null, iconName, true);  }  public boolean equals(Object o) {    if (!(o instanceof CommonButton))      return false;    CommonButton btn = (CommonButton) o;    return btn.funcCtrl == funcCtrl && (iconName == null && btn.iconName == null || (btn.iconName.equals(iconName)))      && (defaultText == null && btn.defaultText == null || (btn.defaultText.equals(defaultText)))      && (funcId == null && btn.funcId == null || (btn.funcId.equals(funcId)));  }  public int hashCode() {    int rtn = super.hashCode();    byte[] bytes = getByte(funcId);    rtn = _getHashCode(rtn, bytes);    bytes = getByte(defaultText);    rtn = _getHashCode(rtn, bytes);    bytes = getByte(iconName);    rtn = _getHashCode(rtn, bytes);    rtn = 37 * rtn + (funcCtrl ? 0 : 1);    return rtn;  }  private int _getHashCode(int rtn, byte[] bytes) {    // TODO Auto-generated method stub    if (rtn == 0 || rtn < 0)      rtn = 17;    if (bytes == null)      return rtn;    for (int i = 0; i < bytes.length; i++) {      rtn = 37 * rtn + (int) bytes[i];    }    return rtn;  }  private byte[] getByte(String str) {    // TODO Auto-generated method stub    if (str == null)      return new byte[] {};    return str.getBytes();  }}