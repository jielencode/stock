package com.ufgov.zc.client.component.ui.fieldeditor;import java.awt.event.KeyAdapter;import java.awt.event.KeyEvent;import javax.swing.JComponent;import javax.swing.JTextField;import com.ufgov.zc.common.cp.model.CpVoucher;public class CheckNoTextFieldEditor extends AbstractFieldEditor {  private JTextField field;  public CheckNoTextFieldEditor(String name) {    super(name);  }  public CheckNoTextFieldEditor(String name, boolean isEditable) {    super(name);    this.field.setEditable(isEditable);  }  public void setEnabled(boolean enabled) {    this.field.setEditable(enabled);  }  public Object getValue() {    return field.getText();  }  //支票号  public void setValue(Object value) { //在选择的上面的行发生改变的时候所触发的事件    if (value == null || value instanceof String) {      field.setText((String) value);      return;    } else if (value instanceof CpVoucher) {      CpVoucher cpVoucher = (CpVoucher) value;      field.setText(cpVoucher.getCheckNo());      return;    }    field.setText(value.toString());  }  protected JComponent createEditorComponent() { //生成下面的控件，并将改变的内容同步到对象中    field = new JTextField();    field.addKeyListener(new KeyAdapter() {      public void keyReleased(KeyEvent e) {        if (getEditObject() instanceof CpVoucher) {          CpVoucher cpVoucher = (CpVoucher) getEditObject();          if (cpVoucher != null) {            String fieldValue = field.getText();            if (fieldValue != null)              cpVoucher.setCheckNo(fieldValue);          }        }      }    });    return field;  }  @Override  public void setFieldEditorCode() {    this.FieldEditorCode = FieldEditorCodeConstants.CHECK_NO;  }}