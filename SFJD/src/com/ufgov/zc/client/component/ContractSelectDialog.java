package com.ufgov.zc.client.component;import java.awt.Dialog;import java.awt.event.MouseAdapter;import java.awt.event.MouseEvent;import java.util.List;import javax.swing.JTable;import javax.swing.table.DefaultTableModel;import com.ufgov.zc.client.common.ServiceFactory;import com.ufgov.zc.client.common.WorkEnv;import com.ufgov.zc.client.datacache.ContractDataCache;import com.ufgov.zc.client.util.GkPreferencesStore;import com.ufgov.zc.common.commonbiz.model.Contract;import com.ufgov.zc.common.commonbiz.publish.IBaseDataServiceDelegate;import com.ufgov.zc.common.system.RequestMeta;public class ContractSelectDialog extends JTableSelectDialog {  public ContractSelectDialog(Dialog dialog, boolean modal,  JButtonTextField triggerField) {    super(dialog, modal, triggerField);  }  public void initTitle() {    this.setTitle("合同");  }  protected void initDataBufferList() {    String baseDataServiceDelegateName = "baseDataServiceDelegate";    IBaseDataServiceDelegate baseDataServiceDelegate = (IBaseDataServiceDelegate) ServiceFactory    .create(IBaseDataServiceDelegate.class, baseDataServiceDelegateName);    int nd = WorkEnv.getInstance().getTransNd();    RequestMeta requestMeta = WorkEnv.getInstance().getRequestMeta();    dataBufferList = ContractDataCache.getContract();    for (int i = 0; i < dataBufferList.size(); i++) {      Contract rowData = (Contract) dataBufferList.get(i);      this.triggerField.getDataMap().put(rowData.getContractCode(), rowData);    }  }  private List genTableData() {    return dataBufferList;  }  public void initSelectTable() {    String[] names = { "合同号", "合同名称", "项目名称", "预算单位", "甲方", "乙方" };    Object[][] data = null;    tableDataList = this.genTableData();    data = new Object[tableDataList.size()][names.length];    for (int i = 0; i < tableDataList.size(); i++) {      Contract rowData = (Contract) tableDataList.get(i);      int col = 0;      data[i][col++] = rowData.getContractCode();      data[i][col++] = rowData.getContractName();      data[i][col++] = rowData.getProjectName();      data[i][col++] = rowData.getCoName();      data[i][col++] = rowData.getPayee();      data[i][col++] = rowData.getPayer();    }    DefaultTableModel model = new DefaultTableModel(data, names) {      public boolean isCellEditable(int row, int colum) {        return false;      }    };    selectTable.setPreferencesKey(this.getClass().getName() + "_selectTable");    selectTable.setPreferenceStore(GkPreferencesStore.preferenceStore());    selectTable.setModel(model);    selectTable.addMouseListener(new MouseAdapter() {      public void mouseClicked(MouseEvent e) {        if (e.getClickCount() == 2) {          doOK();        }      }    });    selectTable.setAutoResizeMode(JTable.AUTO_RESIZE_OFF);  }}