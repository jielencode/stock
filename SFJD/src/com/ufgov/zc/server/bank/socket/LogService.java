package com.ufgov.zc.server.bank.socket;import java.util.Date;import org.apache.log4j.Logger;import com.ufgov.zc.common.system.model.ResultInfo;import com.ufgov.zc.server.system.SpringContext;import com.ufgov.zc.server.system.service.IBankTradeLogService;public class LogService {  private static final Logger logger = Logger.getLogger(LogService.class);  private static IBankTradeLogService bankTradeLogService;  static {    bankTradeLogService = (IBankTradeLogService) SpringContext.getBean("bankTradeLogService");  }  public static void saveLog(ResultInfo result) {    try {      bankTradeLogService.saveLog(result);    } catch (Exception ex) {      logger.error(ex);    }  }  public static void main(String[] args) {    ResultInfo result = new ResultInfo();    result.setTradeDate(new Date());    result.setOriginalMessage("kajdk");    result.setSendMessage("akjdk");    //LogService.saveLog(result, WorkEnv.getInstance().getRequestMeta());  }}